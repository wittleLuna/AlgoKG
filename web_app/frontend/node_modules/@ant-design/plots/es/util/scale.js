var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { get, isArray, isString, set } from '.';
var DEFAULT_DOMAIN = [0, 1];
export default function scale(props) {
    var yField = props.yField, data = props.data;
    if (isArray(data) &&
        isString(yField) &&
        !get(props, 'scale.y.domain') &&
        data.reduce(function (acc, item) { return acc + item[yField]; }, 0) === 0) {
        var newProps = Object.isFrozen(props) ? __assign({}, props) : props;
        set(newProps, 'scale.y.domain', DEFAULT_DOMAIN);
        return newProps;
    }
    return props;
}
